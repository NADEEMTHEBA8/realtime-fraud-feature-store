# Local (host) dbt profile. The Airflow container uses its own copy at
# orchestration/airflow/dags/profiles.yml (host: postgres instead of localhost).
fraud_warehouse:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5434
      user: fraud_admin
      password: changeme_local_only
      dbname: fraud_reference
      schema: silver
      threads: 4
